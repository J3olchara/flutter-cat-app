# 🔔 Push-уведомления для Кототиндер

## Быстрый старт

Push-уведомления через Firebase Cloud Messaging уже интегрированы в приложение!

### Что нужно сделать

1. **Создайте Firebase проект**
   - Перейдите на [Firebase Console](https://console.firebase.google.com/)
   - Создайте новый проект

2. **Добавьте приложения**
   
   **Android:**
   - Package name: `com.kototinder.kototinder`
   - Скачайте `google-services.json`
   - Поместите в `android/app/google-services.json`
   
   **iOS:**
   - Откройте проект в Xcode, найдите Bundle ID
   - Скачайте `GoogleService-Info.plist`
   - Добавьте в Xcode проект (Runner target)
   - Включите capability "Push Notifications"

3. **Запустите приложение**
   ```bash
   flutter run
   ```

4. **Получите FCM токен**
   В консоли появится токен вида:
   ```
   FCM Token: eXaMpLeToKeN...
   ```

5. **Отправьте тестовое уведомление**
   - Firebase Console → Cloud Messaging → Send test message
   - Вставьте токен и отправьте

## 📖 Подробная документация

Смотрите полную инструкцию в [FIREBASE_SETUP.md](./FIREBASE_SETUP.md)

## ⚡ Возможности

- ✅ Получение уведомлений в foreground/background/terminated состояниях
- ✅ Локальные уведомления с настраиваемым каналом
- ✅ Подписка на топики
- ✅ Обработка данных из уведомлений
- ✅ Поддержка Android и iOS
- ✅ Приветственное уведомление через 15 секунд после запуска
- ✅ Re-engagement: уведомление через 30 секунд после закрытия приложения

## 🔧 API

```dart
final messagingService = FirebaseMessagingService();

// Инициализация
await messagingService.initialize();

// Получить токен
final token = await messagingService.getToken();

// Подписаться на топик
await messagingService.subscribeToTopic('cats');

// Отписаться от топика
await messagingService.unsubscribeFromTopic('cats');
```

## 📱 Тестирование

### Через cURL

```bash
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "DEVICE_FCM_TOKEN",
    "notification": {
      "title": "Новый котик! 🐱",
      "body": "Смотри какой милашка!"
    },
    "data": {
      "cat_id": "123",
      "action": "view"
    }
  }'
```

### Отправка на топик

```bash
curl -X POST https://fcm.googleapis.com/fcm/send \
  -H "Authorization: key=YOUR_SERVER_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "/topics/cats",
    "notification": {
      "title": "Новые котики",
      "body": "10 новых котиков добавлено!"
    }
  }'
```

## 🎯 Следующие шаги

1. Добавьте логику навигации при клике на уведомление
2. Интегрируйте с вашим backend для отправки токена
3. Настройте сегментацию пользователей
4. Добавьте аналитику открытий уведомлений

---

**Важно:** Не забудьте добавить конфигурационные файлы Firebase в `.gitignore`!

Они уже добавлены:
```
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
```
